# Личный кабинет - Интеграция с Bitrix24

Система интеграции с Bitrix24 для отображения личного кабинета клиента с данными из CRM. Проект получает webhook-события от Bitrix24, сохраняет данные локально и отображает их в удобном интерфейсе.

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Требования](#требования)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [Структура проекта](#структура-проекта)
- [Использование](#использование)
- [API и Webhooks](#api-и-webhooks)
- [Логирование](#логирование)
- [Разработка](#разработка)

## 📖 Описание

Проект представляет собой интеграцию с Bitrix24, которая:

- **Принимает webhook-события** от Bitrix24 (создание, обновление, удаление контактов, компаний, сделок, проектов)
- **Хранит данные локально** в JSON-файлах для быстрого доступа
- **Синхронизирует данные** между Bitrix24 и локальным хранилищем
- **Поддерживает смарт-процессы** для работы с проектами и специальными операциями

## ✨ Возможности

### Обработка событий Bitrix24
- Создание, обновление и удаление контактов
- Создание, обновление и удаление компаний
- Создание и обновление сделок
- Работа со смарт-процессами (проекты, изменение данных, удаление данных)

### Локальное хранилище
- Кэширование данных в памяти для быстрого доступа
- Хранение в JSON-файлах (контакты, компании, сделки, проекты, менеджеры)
- Автоматическая синхронизация при получении webhook-событий

### Личный кабинет
- Отображение информации о контакте
- Просмотр связанных компаний
- Список сделок и проектов
- Информация о менеджерах
- Автоматический выбор последнего обновленного контакта

## 🔧 Требования

- PHP 7.4 или выше
- Веб-сервер (Apache/Nginx) с поддержкой PHP
- Доступ к Bitrix24 API (webhook URL)
- Права на запись в директории `src/data/` и `src/logs/`

## 📦 Установка

1. **Клонируйте репозиторий или скопируйте файлы проекта**

2. **Настройте права доступа:**
```bash
chmod -R 755 src/data
chmod -R 755 src/logs
```

3. **Создайте файл `.env` в корне проекта** (см. раздел [Конфигурация](#конфигурация))

4. **Настройте веб-сервер:**
   - Укажите корневую директорию проекта
   - Убедитесь, что PHP может выполнять файлы
   - Настройте URL для webhook: `https://yourdomain.com/src/webhooks/bitrix24.php`

## ⚙️ Конфигурация

### Переменные окружения (.env)

Создайте файл `.env` в корне проекта со следующими параметрами:

```env
# Настройки Bitrix24
BITRIX24_WEBHOOK_URL=https://your-portal.bitrix24.ru/rest/1/webhook_code/
BITRIX24_APPLICATION_TOKEN=your_application_token
BITRIX24_TIMEOUT=30

# Настройки логирования
LOG_ENABLED=true
LOG_LEVEL=INFO
```

### Конфигурация в `src/config/bitrix24.php`

Основные настройки находятся в файле конфигурации:

- **ID смарт-процессов** - настройте ID ваших смарт-процессов
- **Маппинг полей** - сопоставление полей Bitrix24 с локальными полями
- **Настройки логирования** - уровень логирования и путь к файлу логов

### Настройка webhook в Bitrix24

1. Перейдите в настройки Bitrix24 → Разработчикам → Другое → Входящий вебхук
2. Создайте вебхук с правами доступа:
   - `crm` (контакты, компании, сделки)
   - `user` (пользователи/менеджеры)
3. Скопируйте URL вебхука в переменную `BITRIX24_WEBHOOK_URL`
4. Настройте обработчик событий в Bitrix24:
   - `ONCRMCONTACTADD` - создание контакта
   - `ONCRMCONTACTUPDATE` - обновление контакта
   - `ONCRMCONTACTDELETE` - удаление контакта
   - `ONCRMCOMPANYADD` - создание компании
   - `ONCRMCOMPANYUPDATE` - обновление компании
   - `ONCRMCOMPANYDELETE` - удаление компании
   - `ONCRMDEALUPDATE` - обновление сделки
   - `ONCRM_DYNAMIC_ITEM_UPDATE` - обновление смарт-процесса

## 📁 Структура проекта

```
lk/
├── index.php                    # Главная страница личного кабинета
├── src/
│   ├── classes/                 # Основные классы
│   │   ├── Bitrix24API.php     # Класс для работы с API Bitrix24
│   │   ├── LocalStorage.php    # Класс для локального хранения данных
│   │   ├── Logger.php           # Класс для логирования
│   │   └── EnvLoader.php       # Загрузка переменных окружения
│   ├── config/
│   │   └── bitrix24.php        # Конфигурация проекта
│   ├── webhooks/
│   │   └── bitrix24.php        # Обработчик webhook-событий
│   ├── scripts/
│   │   ├── test_sync.php       # Скрипт для тестовой синхронизации
│   │   └── test_smart_process_cards.php
│   ├── data/                    # Локальное хранилище данных (JSON)
│   │   ├── contacts.json
│   │   ├── companies.json
│   │   ├── deals.json
│   │   ├── projects.json
│   │   └── managers.json
│   └── logs/                    # Логи приложения
│       └── bitrix24_webhooks.log
├── docs/                        # Документация
└── README.md                    # Этот файл
```

## 🔌 API и Webhooks

### Endpoint для webhook

```
POST /src/webhooks/bitrix24.php
```

Webhook принимает события от Bitrix24 и автоматически:
- Валидирует запрос
- Определяет тип события
- Обрабатывает данные
- Сохраняет в локальное хранилище
- Логирует операции

### Обрабатываемые события

- **Контакты**: `ONCRMCONTACTADD`, `ONCRMCONTACTUPDATE`, `ONCRMCONTACTDELETE`
- **Компании**: `ONCRMCOMPANYADD`, `ONCRMCOMPANYUPDATE`, `ONCRMCOMPANYDELETE`
- **Сделки**: `ONCRMDEALUPDATE`, `ONCRMDEALADD`
- **Смарт-процессы**: `ONCRM_DYNAMIC_ITEM_UPDATE`, `ONCRM_DYNAMIC_ITEM_ADD`

### Класс Bitrix24API

Основные методы:

```php
// Получение данных сущности
$bitrixAPI->getEntityData('contact', $contactId);

// Получение списка сущностей
$bitrixAPI->getEntityList('company', ['filter' => ['CONTACT_ID' => $contactId]]);

// Создание элемента смарт-процесса
$bitrixAPI->addSmartProcessItem($entityTypeId, $fields);
```

### Класс LocalStorage

Основные методы:

```php
// Получение контакта
$localStorage->getContact($contactId);
$localStorage->getLastUpdatedContact();

// Получение всех данных
$localStorage->getAllContacts();
$localStorage->getAllCompanies();
$localStorage->getAllDeals();
$localStorage->getAllProjects();

// Синхронизация
$localStorage->syncContact($lkId, $contactData);
$localStorage->syncCompanyByBitrixId($companyId, $companyData);

// Удаление
$localStorage->deleteContactData($contactId);
```

## 📝 Логирование

### Уровни логирования

- **ERROR** - критические ошибки
- **WARNING** - предупреждения
- **INFO** - важные события (создание, обновление, удаление)
- **DEBUG** - детальная информация (включить только для отладки)

### Настройка уровня логирования

В файле `.env`:
```env
LOG_LEVEL=INFO  # или DEBUG для детального логирования
```

### Просмотр логов

Логи сохраняются в файл `src/logs/bitrix24_webhooks.log`

```bash
tail -f src/logs/bitrix24_webhooks.log
```

## 🛠️ Разработка

### Основные классы

#### LocalStorage
- Управляет локальным хранилищем данных
- Использует кэширование в памяти для оптимизации
- Автоматически создает директории и файлы

#### Bitrix24API
- Обрабатывает запросы к API Bitrix24
- Поддерживает маппинг полей
- Работает со смарт-процессами

#### Logger
- Многоуровневое логирование
- Ротация логов
- Поддержка контекстной информации

### Оптимизации

- **Кэширование данных** - данные кэшируются в памяти при чтении
- **Оптимизированное логирование** - детальные логи только в DEBUG режиме
- **Ленивая загрузка** - данные загружаются только при необходимости

### Добавление новых полей

1. Обновите маппинг в `src/config/bitrix24.php`
2. При необходимости обновите функции отображения в `index.php`
3. Протестируйте синхронизацию

## 🔒 Безопасность

- Webhook-запросы валидируются по токену приложения
- Конфиденциальные данные хранятся в `.env` (не коммитится в репозиторий)
- Логи не содержат чувствительной информации
- Защита директорий через `.htaccess`

## 📄 Лицензия

Проект разработан для внутреннего использования.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в `src/logs/bitrix24_webhooks.log`
2. Убедитесь в правильности настроек в `.env`
3. Проверьте доступность Bitrix24 API
4. Убедитесь в корректности webhook URL в Bitrix24

---

**Версия:** 1.2 
**Последнее обновление:** 23.11.2025

